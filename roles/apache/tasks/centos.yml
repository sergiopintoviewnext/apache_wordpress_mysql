---

- name: Instalar apache centos
  ansible.builtin.yum:
    name: httpd
    state: present
    update_cache: true
    

- name: Servicio httpd
  ansible.builtin.service:
    name: httpd
    state: started
    enabled: true

- name: permitir trafico http
  ansible.posix.firewalld:
    service: http
    permanent: true
    state: enabled

...
